library;

/// Re-export from the shared location so existing imports continue to work
/// while we migrate consumers to the canonical shared path.
export 'package:brewline/shared/widgets/settings/settings_section_card.dart';
